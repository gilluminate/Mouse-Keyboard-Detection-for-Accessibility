Mouse-Keyboard-Detection-for-Accessibility
==========================================

detects keyboard and mouse events to add a classname to the &lt;body> tag. Styles can vary for :focus depending on whether or not the classnames are present.
